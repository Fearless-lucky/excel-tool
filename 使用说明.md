# 月度折旧生成工具

## 交付文件

- 主工具：`月度折旧生成工具.xlsm`
- 主模块源码：`VBA源码\modDepreciation.bas`
- 类模块源码：`VBA源码\CChangeItem.cls`
- 类模块源码：`VBA源码\CResultRow.cls`
- 可直接粘贴到 VBA 编辑器的源码：`VBA源码\*_可直接复制.txt`

工具主要面向 Windows 版 Microsoft Excel，同时兼容 Windows WPS 表格。已在 Microsoft Excel 16.0 和 WPS 12.0 中完成实际运行验证。工具完全离线运行，不联网、不上传数据，不使用数据库、第三方接口、ActiveX 或 UserForm。上月原文件以只读方式打开，生成结果另存为新的 `.xlsx` 文件。

v1.3 已针对 WPS 修复“选择文件后路径和期间未回填”的兼容问题：首页顶部不再依赖 WPS 会错误扩展的相邻合并单元格，选择文件后会稳定显示文件路径、最新月表、上月期间和本月期间。

建议优先使用 Microsoft Excel。若双击文件仍默认由 WPS 打开，可右键 `月度折旧生成工具.xlsm`，选择“打开方式 → Microsoft Excel”；两种软件共用同一个工具文件，不需要分别安装或维护两个版本。

## “操作首页”布局

首页采用 A:M、27 行的横向紧凑布局，保存时默认缩放为 76%；启用宏后会按当前窗口自动适配。在本机 Microsoft Excel 16.0 中自动适配为 78%，标题、10 行变动区、四个按钮、状态栏和隐私说明均可一屏显示。

| 区域 | 单元格 | 内容 |
|---|---|---|
| 标题 | A1:M2 | 工具名称、离线与“不修改源文件”说明 |
| 上月文件 | A4:G7 | 文件路径、最新月度工作表、上月期间、本月期间 |
| 折旧规则 | H4:M7 | 三项下拉规则 |
| 本月资产变动 | A9:M21 | 13 个完整资产字段、10 行输入区及填写提示 |
| 操作按钮 | A23:M25 | 四个等宽主要按钮 |
| 状态与隐私说明 | A26:M27 | 当前状态、离线隐私说明 |

关键输入单元格：

- B5：上月文件路径
- B6：自动识别的最新月度工作表
- E6：上月期间
- G6：本月期间
- J5：新增资产起提时间
- J6：最低净值
- J7：报废或停用当月规则
- A11:M20：本月资产变动
- B26：运行状态

资产变动表字段：

| 列 | 字段 | 说明 |
|---|---|---|
| A | 变动类型 | 无变动、新增、调整、报废、停用 |
| B | 财产编号 | 唯一匹配编号，按文本保存，可保留前导零 |
| C | 固定资产类别 | 新增时必填；调整时选填 |
| D | 计量单位 | 新增时必填；调整时选填 |
| E | 数量 | 新增时空白默认 1；调整时选填 |
| F | 原值 | 新增时必填；调整时选填 |
| G | 使用年限 | 新增时必填；调整时选填 |
| H | 预计净残值 | 空白按 0 处理 |
| I | 月均折旧 | 新增时可留空自动计算 |
| J | 购买日期 | 支持 Excel 日期或“2026年2月”等文字；新增时必填 |
| K | 保存地点 | 新增时必填；调整时选填 |
| L | 使用部门及个人 | 新增时必填；调整时选填 |
| M | 说明 | 写入摘要；调整、报废、停用时用于补充说明 |

新增资产要求填写完整基础资料，目的是使生成后的月度表可直接归档并作为下月源表继续使用，不再需要人工补列。

## 按钮与宏

| 按钮 | 宏名称 |
|---|---|
| 选择上月折旧表 | `SelectSource` |
| 检查并预览 | `CheckAndPreview` |
| 生成本月折旧表 | `GenerateMonth` |
| 清空 | `ClearAll` |

按钮绑定使用 `modDepreciation.宏名称`。窗口适配宏为 `FitHomeView`，不占用首页按钮。

## 使用方法

以 `折旧表_脱敏.xlsx` 为例：

1. 打开 `月度折旧生成工具.xlsm`，点击“启用内容/启用宏”。
   工具会自动运行首页初始化：进入“操作首页”、定位到 A1，并设置为 100% 缩放。
2. 点击“选择上月折旧表”，选择 `折旧表_脱敏.xlsx`。
3. 工具自动识别最新月表 `2026.01`，并显示本月期间 `2026年2月`。
4. 检查三项折旧规则；默认值为“当月开始计提”“最低净值为 0”“当月仍计提折旧”。
5. 没有资产变动时，A11:M20 全部留空。
6. 有变动时，选择变动类型并填写对应字段。
7. 点击“检查并预览”。检查通过后会生成一张完整的“检查预览”月表：保留原月表的格式、15 列、合计行、冻结窗格和打印设置，并模拟本月折旧及资产变动；存在严重错误时会改为显示错误明细并禁止生成。
8. 点击“生成本月折旧表”，选择源文件夹或其他保存文件夹。
9. 输出文件名为 `折旧表_2026.02.xlsx`，源文件保持不变。

Microsoft Excel 首次打开时如出现黄色“安全警告”栏，需要点击“启用内容”。如果文件进入“受保护的视图”，请先确认文件确实来自本交付文件夹，再按 Excel 提示启用编辑和宏。

导入路径、检查预览和状态信息仅在本次使用期间保留。关闭工具时不会提示保存这些临时内容；已生成的本月 `.xlsx` 文件不受影响。

## 从源码创建 XLSM

1. 在 Windows Excel 中新建空白工作簿，将工作表改名为“操作首页”。
2. 按 Alt+F11 打开 VBA 编辑器。
3. 插入标准模块并命名为 `modDepreciation`，粘贴 `modDepreciation_可直接复制.txt`。
4. 插入两个类模块，分别命名为 `CChangeItem`、`CResultRow`，粘贴对应的 `*_可直接复制.txt`。
5. 在 `ThisWorkbook` 中粘贴：

```vb
Option Explicit

Private Sub Workbook_Open()
    On Error Resume Next
    modDepreciation.InitializeHome
    On Error GoTo 0
End Sub

Private Sub Workbook_BeforeClose(Cancel As Boolean)
    On Error Resume Next
    Me.Saved = True
    On Error GoTo 0
End Sub
```

6. 按上方 A:M 布局设置首页，A11:M20 建立资产变动表。
7. 为 J5、J6、J7 设置三项规则下拉，为 A11:A20 设置变动类型下拉。
8. 创建四个普通形状按钮并指定相应宏。
9. 另存为“Excel 启用宏的工作簿”，文件名为 `月度折旧生成工具.xlsm`。
10. 重新打开并启用宏，先用脱敏副本测试。

也可以直接导入 `.bas` 和 `.cls`。两个 `.cls` 必须显示在“类模块”文件夹中；若被错误导入为标准模块，请删除后重新插入类模块并粘贴源码。

## 测试示例

### 无变动

- 不填写资产变动表。
- 预期：0001 本月折旧 64.23，本月末净值 10791.96。

### 完整新增并自动计算

- 变动类型：新增
- 财产编号：0002
- 固定资产类别：通用设备
- 计量单位：台
- 数量：2
- 原值：12000
- 使用年限：5
- 预计净残值：1200
- 月均折旧：留空
- 购买日期：2026-02-01
- 保存地点：设备间
- 使用部门及个人：综合部/张三
- 新增规则：当月开始计提
- 预期：月均折旧 180.00，本月折旧 180.00，本月末净值 11820.00；所有基础资料写入新资产行。

### 新增且次月起提

- 新增规则：次月开始计提
- 其余新增资料完整填写。
- 预期：月均折旧正常计算，本月折旧为 0，本月末净值等于原值。

### 完整调整

- 变动类型：调整
- 财产编号：0001
- 可选填写类别、单位、数量、原值、年限、残值、月均折旧、购买日期、地点、部门或说明。
- 预期：只更新填写项目；空白项目保持不变。

### 报废且当月不计提

- 变动类型：报废
- 财产编号：0001
- 报废或停用当月：当月不计提折旧
- 预期：资产行保留并标记，本月折旧 0，本月末净值 10856.19。

### 错误拦截

- 新增编号填写已存在的 0001：应报告重复编号并禁止生成。
- 新增资产缺少类别、单位、年限、购买日期、地点或部门：应说明具体首页行号并禁止生成。
- 输入非法规则、负数、非数值金额、公式错误或缺少合计行：应禁止生成。

## 已完成验证

- Microsoft Excel 16.0（16.0.20131.20154）真实对象模型与界面验证通过。
- WPS 表格 12.0 兼容验证通过；两套软件使用同一个 `.xlsm` 文件。
- WPS 中“选择上月折旧表 → 检查并预览”连续操作验证通过：文件路径、期间可正确回填并被后续操作读取。
- 检查通过后，“检查预览”为完整的下一月月表副本，包含 A:O 明细列、合计公式及本月变动模拟结果；不会修改上月源文件。
- 检查预览与实际生成文件的期间单元格均按中文月份显示，例如“2026年2月”。
- Microsoft Excel 中首页自动缩放为 78%，A:M 与第 27 行均在一屏内。
- `2026.01 → 2026.02` 月份识别通过。
- Microsoft Excel 和 WPS 中，无变动、完整新增、次月起提、完整调整、报废不计提五类成功场景均通过。
- Microsoft Excel 和 WPS 中，重复编号、缺少新增必填资料均被阻止，未留下输出文件。
- 新增资产 13 个字段完整写入；调整可更新全部基础资料和折旧参数。
- 输出文件为 `.xlsx`，不含 VBA 工程。
- 新月份表保留冻结窗格 B2、横向打印设置、列宽、行高和历史工作表。
- 合计行公式自动扩展到新增资产行。
- 生成完成前会再次自检新月表的严重错误和公式错误。
- Excel 与 WPS 对样式编号、会计数字格式内部写法及列宽小数的保存方式略有不同，但显示内容、折旧数值、公式、冻结窗格和打印方向一致。
- 源样表 SHA-256 在测试前后完全一致。
- 样表的非月度历史台账中原本存在一个 `#REF!` 公式；工具按“不修改历史工作表”的要求原样保留，该错误不是生成过程产生的。
